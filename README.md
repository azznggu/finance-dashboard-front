# 금융 대시보드 - 프론트엔드

React + TypeScript + Vite 기반 실시간 금융 정보 대시보드 웹 애플리케이션입니다.

## 🎨 주요 기능

### 실시간 데이터 시각화
- **환율**: USD/KRW, JPY/KRW
- **금시세**: 3.75그램 (1돈) 기준
- **가상화폐**: BTC, ETH, XRP
- **주가지수**: S&P 500

### 인터랙티브 차트
- Chart.js 기반 실시간 차트
- 기간별 데이터 (1일, 1주, 1개월, 6개월, 1년)
- 변동률 표시 (▲/▼)
- 반응형 디자인

## 🚀 시작하기

### 사전 요구사항
- Node.js 18.0.0 이상
- 백엔드 API 서버 실행 중 (http://localhost:3001)

### 설치
```bash
npm install
```

### 개발 모드 실행
```bash
npm run dev
```
앱이 `http://localhost:5173`에서 실행됩니다.

### 프로덕션 빌드
```bash
npm run build
npm run preview
```

## 🛠️ 기술 스택

- **React 19**: UI 라이브러리
- **TypeScript**: 타입 안정성
- **Vite**: 빌드 도구 (빠른 HMR)
- **Tailwind CSS**: 유틸리티 기반 스타일링
- **Chart.js**: 차트 라이브러리
- **React-Chartjs-2**: Chart.js React 래퍼
- **Axios**: HTTP 클라이언트

## 📁 프로젝트 구조

```
frontend/
├── src/
│   ├── App.tsx              # 메인 대시보드 컴포넌트
│   ├── main.tsx             # 앱 엔트리 포인트
│   ├── index.css            # Tailwind CSS 설정
│   ├── services/
│   │   └── api.ts           # 백엔드 API 클라이언트
│   ├── types/
│   │   └── index.ts         # TypeScript 타입 정의
│   └── components/          # 재사용 가능한 컴포넌트
├── public/                  # 정적 파일
├── index.html               # HTML 템플릿
├── vite.config.ts           # Vite 설정
├── tailwind.config.js       # Tailwind CSS 설정
├── tsconfig.json            # TypeScript 설정
└── package.json
```

## 🎨 컴포넌트 구조

### App.tsx
메인 대시보드 컴포넌트로 다음을 포함:

1. **헤더**
   - 제목 및 설명

2. **기간 선택 버튼**
   - 1일, 1주, 1개월, 6개월, 1년

3. **데이터 섹션**
   - 환율 (2개 카드)
   - 금시세 (1개 카드)
   - 가상화폐 (3개 카드)
   - S&P 500 (1개 카드)

4. **카드 컴포넌트** (각 데이터마다)
   - 제목
   - 현재 가격
   - 변동률
   - 인터랙티브 차트

## 🎯 주요 기능 설명

### 1. 데이터 로딩
```typescript
useEffect(() => {
  loadData();
}, [period]);
```
- 기간이 변경될 때마다 데이터 재로딩
- 7개의 API를 병렬로 호출 (Promise.all)

### 2. 차트 렌더링
- Chart.js Line 차트 사용
- 각 데이터 카테고리마다 고유 색상
- 반응형 차트 (maintainAspectRatio: false)
- 커스텀 툴팁

### 3. 반응형 레이아웃
```css
grid-cols-1          /* 모바일: 1열 */
md:grid-cols-2       /* 태블릿: 2열 (환율) */
md:grid-cols-3       /* 데스크톱: 3열 (가상화폐) */
```

### 4. 로딩 상태
- 스피너 애니메이션
- 데이터 로딩 중 표시

## 🔌 API 연동

### API 클라이언트 (services/api.ts)
```typescript
const API_BASE_URL = 'http://localhost:3001/api';

api.getExchangeRate('USD/KRW', '1day')
api.getGoldPrice('1week')
api.getCryptoPrice('BTC', '1month')
api.getSP500('1year')
```

### 환경별 API URL 설정
개발/프로덕션 환경에 따라 API URL 변경:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001/api';
```

`.env` 파일 생성:
```
VITE_API_URL=https://your-production-api.com/api
```

## 🎨 스타일링

### Tailwind CSS
유틸리티 클래스 기반 스타일링:

```typescript
className="bg-slate-800 rounded-lg p-6 shadow-lg hover:shadow-xl transition-shadow"
```

### 다크 테마
- 배경: `bg-slate-900`
- 카드: `bg-slate-800`
- 텍스트: `text-white`, `text-slate-400`
- 그리드: `#334155`

### 색상 팔레트
- USD/KRW: `#3b82f6` (파랑)
- JPY/KRW: `#8b5cf6` (보라)
- 금: `#f59e0b` (주황)
- BTC: `#f97316` (진한 주황)
- ETH: `#6366f1` (인디고)
- XRP: `#14b8a6` (청록)
- S&P500: `#10b981` (초록)

## 📊 차트 옵션

### 공통 옵션
```typescript
{
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: { display: false },
    tooltip: { /* 커스텀 */ }
  },
  scales: {
    x: { grid: { color: '#334155' } },
    y: { grid: { color: '#334155' } }
  }
}
```

### 툴팁 커스터마이징
```typescript
callbacks: {
  label: function(context) {
    return `${context.parsed.y.toLocaleString('ko-KR')} ${unit}`;
  }
}
```

## 🐛 디버깅

### 개발자 도구
```bash
npm run dev
```
- 브라우저 콘솔에서 API 응답 확인
- React DevTools로 컴포넌트 상태 확인

### 일반적인 문제

#### 1. API 연결 실패
```
Error: Network Error
```
**해결책:**
- 백엔드 서버가 실행 중인지 확인 (`http://localhost:3001/health`)
- CORS 에러인 경우 백엔드 CORS 설정 확인

#### 2. 차트가 표시되지 않음
```
Warning: Canvas is already in use
```
**해결책:**
- 컴포넌트가 두 번 렌더링되는지 확인
- Chart.js가 제대로 등록되었는지 확인

#### 3. 스타일이 적용되지 않음
**해결책:**
```bash
npm install -D tailwindcss postcss autoprefixer
```

## 🚀 배포

### Vercel
```bash
npm run build
# dist 폴더를 Vercel에 배포
```

### Netlify
```bash
npm run build
# dist 폴더를 Netlify에 배포
```

### 환경 변수 설정
배포 시 환경 변수 추가:
```
VITE_API_URL=https://your-api-domain.com/api
```

## 📱 반응형 디자인

### 브레이크포인트
- **모바일**: < 768px (1열 레이아웃)
- **태블릿**: ≥ 768px (2열 레이아웃)
- **데스크톱**: ≥ 1024px (3열 레이아웃, 가상화폐 섹션)

### 테스트
```bash
# Chrome DevTools에서 다양한 기기 크기 테스트
- iPhone SE
- iPad
- Desktop (1920x1080)
```

## ⚡ 성능 최적화

### 1. 코드 스플리팅
Vite가 자동으로 처리

### 2. 메모이제이션
필요시 `useMemo`, `useCallback` 추가

### 3. 이미지 최적화
현재 이미지 없음 (아이콘은 emoji 사용)

## 🔐 보안

### XSS 방지
React는 기본적으로 XSS 방지 (JSX 자동 이스케이프)

### API 키 보호
환경 변수로 관리 (`.env` 파일은 `.gitignore`에 추가)

## 📝 라이선스

MIT License

## 🤝 기여

이슈와 PR은 언제나 환영합니다!
